<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteBase /

    # Let existing files pass through
    RewriteCond %{REQUEST_FILENAME} -f [OR]
    RewriteCond %{REQUEST_FILENAME} -d
    RewriteRule ^ - [L]

    # Everything else → index.html
    RewriteRule . /index.html [L]
</IfModule>
